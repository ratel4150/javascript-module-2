const zeroFuel = (distanceToPump, mpg, fuelLeft) => {
    if((distanceToPump/2)===mpg&&distanceToPump/mpg===fuelLeft){
      return true
    }else{
      return false
    }
    // TODO
  };

  /* Falta en prolog NASM COBOL*/